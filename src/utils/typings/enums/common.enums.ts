export enum ThemeMode {
    LIGHT = "light",
    DARK = "dark"
}

export enum Locale {
    EN = "en",
    UK = "uk"
}

export enum LocalStorage {
    SESSION = "session"
}

export enum AuthFormType {
    LOGIN = "login",
    REGISTER = "register"
}

export enum Field {
    Name = "name",
    EMAIL = "email",
    PASSWORD = "password",
    REPEAT_PASSWORD = "repeatPassword",
    ADDRESS = "address"
}
