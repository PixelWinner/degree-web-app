import styled from "styled-components";

import { Box } from "@mui/material";

export const ModalButtonsContainer = styled(Box)`
    display: flex;
    flex-direction: row;
    justify-content: flex-end;
    align-items: center;
    gap: 16px;
    width: 100%;
`;
